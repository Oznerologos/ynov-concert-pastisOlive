{% extends 'base.html.twig' %}

{% block title %}Newsletter{% endblock %}

{% block body %}
    <h1>Newsletter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newsletter.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ newsletter.email }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ newsletter.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('newsletter_index') }}">back to list</a>

    <a href="{{ path('newsletter_edit', {'id': newsletter.id}) }}">edit</a>

    {{ include('newsletter/_delete_form.html.twig') }}
{% endblock %}
